import React, {PropTypes, Component} from 'react';
import Button from 'react-materialize';

export default class TeamInfo extends Component {
    render() {
        return (
            <Button>TeamInfo Button!</Button>
        )
    }
}
