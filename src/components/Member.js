import React, {PropTypes, Component} from 'react';
import Button from 'react-materialize';

export default class Member extends Component {
    render() {
        return (
            <Button>Member Button!</Button>
        )
    }
}
