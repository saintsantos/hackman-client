import React, {PropTypes, Component} from 'react';
import Button from 'react-materialize';

export default class TeamList extends Component {
    render() {
        return (
            <Button>Team List Button!</Button>
        )
    }
}
