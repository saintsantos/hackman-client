import React, {PropTypes, Component} from 'react';
import Button from 'react-materialize';

export default class MemberList extends Component {
    render() {
        return (
            <Button>MemberList Button!</Button>
        )
    }
}
